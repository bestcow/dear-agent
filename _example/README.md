# _example — 데모 워크스페이스

양식이 실제로 채워진 모습을 보여주는 데모다. 분류 폴더 `apps/` 아래 프로젝트 2개가 있고, `_INDEX.md`·`OVERVIEW.md`는 루트에서 `build-index.ps1 -Root _example`(또는 `python3 build-index.py --root _example`)로 생성했다.

실제 워크스페이스를 만들 땐 [USAGE.md](../USAGE.md)를 따르고 **이 폴더는 삭제**한다(1단계).
