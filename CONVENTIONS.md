# 문서 규약 (CONVENTIONS)

이 문서가 이 워크스페이스 문서 체계의 **단일 기준**이다. `CLAUDE.md`가 이 규약을 가리킨다.

---

## 1. 적용 범위 — work vs meta

워크스페이스 최상위 폴더는 둘 중 하나다.

- **work** — 작업 대상(분류 폴더와 그 안의 프로젝트). 문서 규약이 **여기에만** 적용된다.
- **meta** — 체계 자체. `CONVENTIONS.md` · `OVERVIEW.md` · `CLAUDE.md` · `README.md` · `USAGE.md`(구축 후 삭제) · `_templates/` · `_example/`(데모, 구축 시 삭제) · `build-index.ps1`·`build-index.py`(목차 생성기) · `.claude/`(스킬·훅·설정 — 규약 강제 계층) · `.gitignore` · `LICENSE` · `.github/`. 규약 적용·스캔 대상이 **아니다.**

> 이 워크스페이스 밖의 폴더(별도 도구·지식베이스 등)는 규약 대상이 아니다 — 각자 자기 문서를 가진다.

## 2. 폴더 분류 — 내용으로 정한다 (이름 아님)

- **프로젝트(잎)** = 그 자체가 작업 대상인 폴더 → `PLAN.md` · `HANDOFF.md` · `LOG.md` · `README.md` · `CLAUDE.md` **5종 모두**.
- **컨테이너** = 안에 프로젝트들을 담은 폴더 → `_INDEX.md` (생성물).

분류 폴더는 프로젝트를 품으므로 컨테이너다. 그 안의 각 폴더가 잎이다. 잎이 하위 프로젝트를 갖게 되면 개념상 그때 컨테이너가 된다 — 단, **목차 생성기는 현재 분류→프로젝트 2단계까지만 스캔**하며, 더 깊은 중첩은 경고로 알려주고 목차에서 제외한다(필요해지면 생성기를 확장한다).

> **구조는 미리 깔지 않는다.** 폴더 계층은 생겨나는 프로젝트의 내용에 따라 형성된다. 특별한 요구가 없으면 일반적인 계층 구조를 따르고, 프로젝트가 특정 아키텍처를 요구하면 그 구조에 맞는 폴더 계층을 만든다.

## 3. 문서별 갱신 트리거

| 문서 | 위치 | 무엇 | 갱신 시점 |
|---|---|---|---|
| `PLAN.md` | 프로젝트 | 목표·범위, 기술 결정, 마일스톤 | 목표·범위·기술 결정이 **바뀔 때** |
| `HANDOFF.md` | 프로젝트 | 마지막 작업 / 다음 할 일 / 막힌 것 + **상태(frontmatter)** | **작업 세션 끝낼 때마다** |
| `LOG.md` | 프로젝트 | 날짜별 굵직한 변경 | 기능 완성·버그 수정·버전업마다 한 줄 |
| `README.md` | 프로젝트 | 소개·실행법·환경변수 | 실행법·의존성이 바뀔 때 |
| `CLAUDE.md` | 프로젝트 | 에이전트 운영 가이드 — 명령·코드지도·함정 | 빌드·실행·구조·함정이 바뀔 때 |
| `_INDEX.md` | 컨테이너 | 분류 내 프로젝트 목록 + 한 줄 상태 | **생성물** — 직접 편집 금지 (§4) |
| `OVERVIEW.md` | 루트 | 전체 프로젝트 지도 | **생성물** — 직접 편집 금지 (§4) |

## 4. 단일 출처 — 상태는 한 번만 쓴다

상태의 **유일한 원본은 각 프로젝트 `HANDOFF.md`의 frontmatter**다. `_INDEX.md`·`OVERVIEW.md`는 그걸 긁어 만드는 **파생물**이며 사람이 직접 쓰지 않는다.

```
HANDOFF.md (frontmatter = 원본)
   └─(스캔→생성)→ _INDEX.md (컨테이너)
        └─(스캔→생성)→ OVERVIEW.md (루트)
```

`_INDEX.md`·`OVERVIEW.md`를 손으로 고치지 말 것. 폴더 구조나 상태가 바뀌면 **HANDOFF를 고친 뒤 목차를 재생성**한다(스캔이 frontmatter만 읽으므로 결정적). 재생성은 루트의 `build-index.ps1`(PowerShell 없으면 `python3 build-index.py`, 동일 출력)이 수행한다 — 컨테이너는 자동 탐색하며, 표·카운트는 이 생성기만 쓴다(`<!-- AUTO:START -->`…`<!-- AUTO:END -->` 안). 마커 밖의 큐레이션 메모(`> 주의:` 등)는 사람이 쓰며 생성기가 보존한다.

## 5. HANDOFF frontmatter 스키마

```yaml
---
status: 개발            # 기획 | 개발 | 운영 | 보류
updated: <YYYY-MM-DD>   # 세션 끝낼 때 갱신
summary: 한 줄 현황      # _INDEX/OVERVIEW 한 줄의 근거
repo: <org>/<name>      # 원격 없으면 이 줄 삭제
---
```

- **기획** = 코드 전, 구상 단계 · **개발** = 만드는 중 · **운영** = 라이브/사용 중(완성됐어도 쓰이면 운영 — 유지보수 빈도 무관) · **보류** = 멈춤(미완·중단·대체·폐기).
- 영어 별칭 허용: `planning`→기획 · `building`→개발 · `live`→운영 · `paused`→보류 (생성기가 정규화). frontmatter엔 값만 쓴다 — 인라인 `#` 주석은 파서가 안 걷어내니 넣지 말 것.

## 6. 문서 경계 (중복 금지)

- **README** = 실행법·환경변수·의존성 (사람이 어떻게 돌리나).
- **PLAN** = 목표·범위·의사결정 (왜·무엇을).
- **HANDOFF** = 현재 상태 (지금 어디까지·다음·막힌 것).
- **CLAUDE** = 에이전트 운영 (명령·코드지도·함정 — 클로드가 무엇을 알아야 하나).

## 7. 새 프로젝트

`_templates/`의 **5종(.md)을 복사**해 채운다. 최소 `PLAN`·`HANDOFF`·`CLAUDE`를 먼저 채운다(나머지는 내용 생기면). 빈 섹션은 `_해당 없음_`으로 둬 "미작성"과 구분한다.

> **복사 대상은 `_templates/`의 `.md` 파일뿐.** `.remember/`(remember 툴 스크래치)·`.claude/`(워크트리·로컬설정)는 템플릿이 아니다 — 복사하지 않는다.

## 8. 스캔 제외

목차 생성 스캔은 work(분류 폴더)만 본다. `_templates/`·루트 meta 파일은 제외.

## 9. 강제 계층

규약은 세 겹으로 강제된다. 셋 다 repo에 포함돼 template를 받은 쪽에서도 동일 작동한다.

| 계층 | 파일 | 역할 |
|---|---|---|
| 컨텍스트 | 루트 `CLAUDE.md` | 규약 존재와 트리거를 상시 노출 |
| 스킬 | `.claude/skills/dear-agent/SKILL.md` | 시작·종료·새 프로젝트·구조 변경 절차 (`/handoff` 커맨드 포함) |
| 훅 | `.claude/settings.json` + `.claude/hooks/*.py` | SessionStart: 규약+현황 컨텍스트 주입 · Stop: HANDOFF가 프로젝트 파일보다 오래되면 종료 차단 |

- 훅은 Python 3.7+(표준 라이브러리만)로 실행된다. Python이 없으면 훅만 조용히 비활성 — 스킬·CLAUDE.md 계층은 그대로 작동한다.
- 처음 여는 사용자는 Claude Code의 프로젝트 설정(훅) 승인 프롬프트에 동의해야 훅이 작동한다.

---

## 구조 변경 기록

- _<YYYY-MM-DD>_ — 워크스페이스 생성, 본 규약 도입.
